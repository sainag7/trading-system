/// <reference types="vite/client" />

// Injected by Vite's `define` at build time (see web/vite.config.ts). Used to
// detect that this bundle is newer than the server process serving it.
declare const __BUILD_TIME__: string;
